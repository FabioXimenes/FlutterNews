import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:flutter_news_app/app/modules/start/submodules/categories/categories_page.dart';

void main() {
  testWidgets('CategoriesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CategoriesPage(title: 'Categories')));
    //  final titleFinder = find.text('Categories');
    //  expect(titleFinder, findsOneWidget);
  });
}
